# ============================================================================
# JobMatch AI Environment Configuration
# ============================================================================
# 
# ⚠️  IMPORTANT: SECRETS MUST USE GOOGLE SECRET MANAGER IN PRODUCTION
# 
# This .env.example file is for LOCAL DEVELOPMENT ONLY.
# 
# In production (on GCP VM or Cloud Run), secrets are loaded from
# Google Secret Manager. See: backend/src/config/secrets.ts
# 
# To set up Secret Manager, run: scripts/setup-secrets.sh
# 
# ============================================================================

# Server Configuration
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database
# LOCAL ONLY - Production uses Secret Manager: jobmatch-database-url
DATABASE_URL=postgresql://user:password@localhost:5432/jobmatch_ai

# JWT Authentication
# LOCAL ONLY - Production uses Secret Manager: jobmatch-jwt-secret
# Generate with: openssl rand -base64 32
JWT_SECRET=your-local-dev-secret-key-here
JWT_EXPIRES_IN=7d

# OpenAI API
# LOCAL ONLY - Production uses Secret Manager: jobmatch-openai-key
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-local-dev-key-here

# Email Service (optional for local development)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password

# Logging
LOG_LEVEL=info

# ============================================================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================================================
# 
# DO NOT set JWT_SECRET, DATABASE_URL, or OPENAI_API_KEY in production .env
# 
# Instead, configure them in Google Secret Manager:
# 
# 1. Run the setup script:
#    bash scripts/setup-secrets.sh
# 
# 2. Add your OpenAI API key:
#    echo -n 'sk-proj-YOUR_KEY' | gcloud secrets create jobmatch-openai-key \
#      --project=futurelink-private-112912460 \
#      --replication-policy='automatic' \
#      --data-file=-
# 
# 3. Secrets will be automatically loaded when NODE_ENV=production
# 
# Secret names used:
#   - jobmatch-jwt-secret
#   - jobmatch-database-url  
#   - jobmatch-openai-key
# 
# ============================================================================
