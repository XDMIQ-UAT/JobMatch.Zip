name: Deploy jobmatch.zip to Vercel (CI/CD)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  # Checks that must pass before deployment
  checks:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies (workspace)
        run: |
          npm ci --ignore-scripts 2>&1 | tee install.log || {
            echo "⚠️ npm ci failed, trying npm install..."
            npm install --ignore-scripts --legacy-peer-deps || true
          }
          # Skip Playwright installation if it fails
          if grep -q "playwright" install.log; then
            echo "⚠️ Playwright installation skipped (not required for deployment)"
          fi
          
          # Ensure frontend has React installed for TypeScript resolution
          echo "Setting up frontend node_modules for TypeScript..."
          cd frontend
          
          # Install React dependencies directly in frontend if not present
          if [ ! -d "node_modules/react" ] || [ ! -d "node_modules/react-dom" ] || [ ! -d "node_modules/react-router-dom" ]; then
            echo "Installing React dependencies in frontend..."
            npm install --ignore-scripts --legacy-peer-deps --no-save react@^18.2.0 react-dom@^18.2.0 react-router-dom@^7.9.6 @types/react@^18.2.46 @types/react-dom@^18.2.18 2>&1 || {
              echo "⚠️ Direct install failed, trying with workspace flag..."
              npm install --workspace=frontend --ignore-scripts --legacy-peer-deps react react-dom react-router-dom @types/react @types/react-dom 2>&1 || true
            }
          fi
          
          # Verify React installation
          echo "Verifying React installation..."
          if [ -d "node_modules/react" ]; then
            echo "✅ React found in frontend/node_modules"
          else
            echo "❌ React NOT found in frontend/node_modules"
            echo "Attempting to copy from root..."
            if [ -d "../node_modules/react" ]; then
              mkdir -p node_modules
              cp -r ../node_modules/react node_modules/ 2>/dev/null || true
              cp -r ../node_modules/react-dom node_modules/ 2>/dev/null || true
              cp -r ../node_modules/react-router-dom node_modules/ 2>/dev/null || true
            fi
          fi
          
          # Verify React types
          if [ -d "node_modules/@types/react" ]; then
            echo "✅ React types found in frontend/node_modules/@types"
          else
            echo "⚠️ React types NOT found, copying from root..."
            if [ -d "../node_modules/@types/react" ]; then
              mkdir -p node_modules/@types
              cp -r ../node_modules/@types/react node_modules/@types/ 2>/dev/null || true
              cp -r ../node_modules/@types/react-dom node_modules/@types/ 2>/dev/null || true
            fi
          fi
          
          cd ..
          
          # Final verification
          echo "Final React installation check..."
          ls -la frontend/node_modules/react/package.json 2>/dev/null && echo "✅ React package.json found" || echo "❌ React package.json NOT found"
          ls -la frontend/node_modules/react/jsx-runtime.js 2>/dev/null && echo "✅ React jsx-runtime found" || echo "❌ React jsx-runtime NOT found"
          ls -la frontend/node_modules/@types/react/index.d.ts 2>/dev/null && echo "✅ React types found" || echo "❌ React types NOT found"

      - name: Lint frontend
        working-directory: ./frontend
        run: npm run lint

      - name: Lint backend
        working-directory: ./backend
        run: npm run lint

      - name: Type check frontend
        working-directory: ./frontend
        run: |
          # Verify React is accessible before typecheck
          echo "Verifying React installation for TypeScript..."
          ls -la node_modules/react/package.json 2>/dev/null && echo "✅ React package.json found" || echo "❌ React package.json NOT found"
          ls -la node_modules/react/jsx-runtime.js 2>/dev/null && echo "✅ React jsx-runtime found" || echo "❌ React jsx-runtime NOT found"
          ls -la node_modules/@types/react/index.d.ts 2>/dev/null && echo "✅ React types found" || echo "❌ React types NOT found"
          # Run typecheck
          npm run typecheck

      - name: Generate Prisma Client
        working-directory: ./backend
        run: npm run generate

      - name: Type check backend
        working-directory: ./backend
        run: npm run typecheck

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      - name: Build backend
        working-directory: ./backend
        run: |
          npm run generate
          npm run build

      - name: Verify builds
        run: |
          if [ ! -d "frontend/dist" ]; then
            echo "❌ Frontend build failed"
            exit 1
          fi
          if [ ! -d "backend/dist" ]; then
            echo "❌ Backend build failed"
            exit 1
          fi
          echo "✅ All builds successful"

  # Deploy to Vercel (unified frontend + backend)
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    needs: checks
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies (workspace)
        run: |
          npm ci --ignore-scripts 2>&1 | tee install.log || {
            echo "⚠️ npm ci failed, trying npm install..."
            npm install --ignore-scripts --legacy-peer-deps || true
          }
          # Skip Playwright installation if it fails
          if grep -q "playwright" install.log; then
            echo "⚠️ Playwright installation skipped (not required for deployment)"
          fi

      - name: Generate Prisma Client
        working-directory: ./backend
        run: npm run generate

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      - name: Build backend
        working-directory: ./backend
        run: npm run build

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Verify Vercel Configuration
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          echo "Checking Vercel secrets..."
          echo ""
          
          # Check VERCEL_TOKEN
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "❌ VERCEL_TOKEN is not set"
            echo "   → Go to: Settings → Secrets and variables → Actions"
            echo "   → Add secret named exactly: VERCEL_TOKEN"
            exit 1
          else
            TOKEN_LENGTH=${#VERCEL_TOKEN}
            echo "✅ VERCEL_TOKEN is set (length: $TOKEN_LENGTH)"
          fi
          
          # Check VERCEL_ORG_ID
          if [ -z "$VERCEL_ORG_ID" ]; then
            echo "❌ VERCEL_ORG_ID is not set"
            echo "   → Go to: Settings → Secrets and variables → Actions"
            echo "   → Add secret named exactly: VERCEL_ORG_ID"
            exit 1
          else
            echo "✅ VERCEL_ORG_ID is set: $VERCEL_ORG_ID"
          fi
          
          # Check VERCEL_PROJECT_ID
          if [ -z "$VERCEL_PROJECT_ID" ]; then
            echo "❌ VERCEL_PROJECT_ID is not set or is empty"
            echo ""
            echo "Troubleshooting steps:"
            echo "1. Go to: Settings → Secrets and variables → Actions"
            echo "2. Verify secret exists with exact name: VERCEL_PROJECT_ID"
            echo "3. Check for typos or extra spaces in the secret name"
            echo "4. Ensure secret value is not empty (should be a project ID string)"
            echo "5. Delete and recreate the secret if needed"
            echo ""
            echo "To find your Project ID:"
            echo "  - Vercel Dashboard → Your Project → Settings → General → Project ID"
            echo "  - Or run locally: vercel link && cat .vercel/project.json"
            exit 1
          else
            echo "✅ VERCEL_PROJECT_ID is set: $VERCEL_PROJECT_ID"
          fi
          
          echo ""
          echo "All Vercel secrets are configured ✅"

      - name: Deploy to Vercel Production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          echo "Verifying Vercel authentication..."
          TOKEN_LENGTH=${#VERCEL_TOKEN}
          echo "Token length: $TOKEN_LENGTH"
          
          # Check token length first (Vercel tokens are typically 24+ characters)
          if [ "$TOKEN_LENGTH" -lt 20 ]; then
            echo "❌ Token appears to be truncated (length: $TOKEN_LENGTH)"
            echo "Vercel tokens are typically 24+ characters"
            exit 1
          fi
          
          # Set project context (required for vercel CLI)
          if [ -n "$VERCEL_PROJECT_ID" ] && [ -n "$VERCEL_ORG_ID" ]; then
            echo "Setting Vercel project context..."
            # Create .vercel directory and project.json if it doesn't exist
            mkdir -p .vercel
            echo "{\"projectId\":\"$VERCEL_PROJECT_ID\",\"orgId\":\"$VERCEL_ORG_ID\"}" > .vercel/project.json
            echo "✅ Project context set"
          fi
          
          # Test token validity (non-blocking - deployment will fail if token is invalid)
          echo "Testing Vercel authentication..."
          if vercel whoami > /dev/null 2>&1; then
            echo "✅ Vercel token is valid"
            vercel whoami
          else
            echo "⚠️  Warning: vercel whoami failed, but continuing deployment..."
            echo "   (Deployment will fail if token is actually invalid)"
            echo "   Token length: $TOKEN_LENGTH"
          fi
          
          echo ""
          echo "Deploying to Vercel production..."
          vercel --prod --yes

      - name: Deployment Summary
        run: |
          echo "✅ Application deployed to Vercel (jobmatch.zip)"
          echo "🌐 Production URL: https://jobmatch.zip"
          echo "🔗 API available at: https://jobmatch.zip/api"

